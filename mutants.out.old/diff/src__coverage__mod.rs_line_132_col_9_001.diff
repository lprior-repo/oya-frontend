--- src/coverage/mod.rs
+++ replace CoverageAnalyzer::find_spec_files -> Result<Vec<PathBuf>, CoverageError> with Ok(vec![Default::default()])
@@ -124,19 +124,17 @@
             total_edge_cases,
             covered_behaviors,
             covered_edge_cases,
             common_gaps,
         })
     }
 
     fn find_spec_files(&self) -> Result<Vec<PathBuf>, CoverageError> {
-        let mut specs = self.collect_yaml_files(&self.specs_dir)?;
-        specs.sort();
-        Ok(specs)
+        Ok(vec![Default::default()]) /* ~ changed by cargo-mutants ~ */
     }
 
     fn collect_yaml_files(&self, root: &Path) -> Result<Vec<PathBuf>, CoverageError> {
         let mut files = Vec::new();
         if !root.exists() {
             return Ok(files);
         }
 
