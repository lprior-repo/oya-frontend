// NOTE: This module is intentionally empty.
// The canonical `ScenarioError` is defined in `types.rs` and re-exported via `mod.rs`.
// This file was a dead-code duplicate (never imported) and has been removed.
// If additional error variants are needed, add them to `types::ScenarioError`.
