scenario:
  id: ext-bundle-semantics
  spec_ref: flow-wasm-v1
  spec_version: 1.0.0
  category: extension-behavior
  visibility: open
  priority: medium
  description: Bundle semantics preserve extension grouping metadata.
  rationale: Bundled recommendations should remain coherent through runner execution.
setup:
  universe: local
  initial_state: extension-bundle-available
  preconditions:
    - description: Extension bundle metadata exists.
      check:
        type: static
        bundle_ready: true
steps:
  - id: verify-bundle-semantics
    description: Validate extension bundle semantic constraints.
    action:
      type: noop
    assertions:
      - type: status
        expected: 0
        message: Bundle semantic check executes without runtime failure.
    extractions: []
teardown:
  reset_universe: true
  custom_cleanup: []
