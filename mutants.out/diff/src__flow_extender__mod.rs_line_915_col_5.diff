--- src/flow_extender/mod.rs
+++ replace apply_reliability_bundle -> Result<AppliedExtension, String> with Ok(Default::default())
@@ -907,26 +907,17 @@
 
     created_nodes
 }
 
 fn apply_reliability_bundle(
     workflow: &mut Workflow,
     key: &str,
 ) -> Result<AppliedExtension, String> {
-    let mut created_nodes = Vec::new();
-    for part in reliability_bundle_members() {
-        let applied = apply_extension(workflow, part.as_str())?;
-        created_nodes.extend(applied.created_nodes);
-    }
-
-    Ok(AppliedExtension {
-        key: key.to_string(),
-        created_nodes,
-    })
+    Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
 }
 
 fn annotate_extension_nodes(
     workflow: &mut Workflow,
     key: ExtensionKey,
     fingerprint: &str,
     node_ids: &[NodeId],
 ) {
